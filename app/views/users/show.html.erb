<% content_for :title, "Stakkd - #{@user.username}" %>

<% if @user.private? && current_user != @user %>
  <%= render "private_profile", user: @user %>
<% else %>
  <%= render "public_profile", user: @user %>
<% end %>
