<% content_for :title, "Stakkd - #{@show.translated_title} - New season" %>

<%= render "header", show: @show %>

<div class="max-w-7xl mx-auto my-8 px-6">
  <%= render "form", season: @season, show: @show %>
</div>
