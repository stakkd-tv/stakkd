<% content_for :title, "Stakkd - Edit #{@show.translated_title} - Season #{@season.number}" %>

<%= render "header", show: @show, season: @season %>

<div class="max-w-7xl mx-auto my-8 px-6">
  <%= render "form", season: @season, show: @show %>
</div>
