<% content_for :title, "Stakkd - Edit #{@franchise.translated_title}" %>

<%= render "header", franchise: @franchise %>

<div class="max-w-7xl mx-auto my-8 px-6">
  <%= render "form", franchise: @franchise %>
</div>
