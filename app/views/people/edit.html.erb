<% content_for :title, "Stakkd - Edit #{@person.translated_name}" %>

<%= render "header", person: @person %>

<div class="max-w-7xl mx-auto my-8 px-6">
  <%= render "form", person: @person %>
</div>
