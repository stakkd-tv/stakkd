<% if person.persisted? %>
  <div class="form-nav">
    <%= link_to "Details", edit_person_path(person), data: {active: action_name == "edit"} %>
    <%= link_to "Images", images_person_path(person), data: {active: action_name == "images"} %>
  </div>
<% end %>
